// Defining the sum function that adds two numbers
function sum(num1, num2) {
    return num1 + num2;
}

// Calling the function with arguments 10 and 20
const output = sum(10, 20);

// Logging the result to the console
console.log(output); // Output: 30